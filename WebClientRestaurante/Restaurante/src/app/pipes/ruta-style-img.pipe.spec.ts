import { RutaStyleImgPipe } from './ruta-style-img.pipe';

describe('RutaStyleImgPipe', () => {
  it('create an instance', () => {
    const pipe = new RutaStyleImgPipe();
    expect(pipe).toBeTruthy();
  });
});
